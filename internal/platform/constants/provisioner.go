package constants

// Tenant RBAC object names created by the provisioner in each tenant namespace.
const (
	TenantRoleName        = "openbao-operator-tenant-role"
	TenantRoleBindingName = "openbao-operator-tenant-rolebinding"
)
