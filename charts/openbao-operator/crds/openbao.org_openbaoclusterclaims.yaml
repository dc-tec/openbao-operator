---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openbaoclusterclaims.openbao.org
spec:
  group: openbao.org
  names:
    kind: OpenBaoClusterClaim
    listKind: OpenBaoClusterClaimList
    plural: openbaoclusterclaims
    singular: openbaoclusterclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantRef.name
      name: Tenant
      type: string
    - jsonPath: .spec.serviceOfferingRef.name
      name: Offering
      type: string
    - jsonPath: .spec.serviceProfileRef.name
      name: Profile
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoClusterClaim is the user-facing namespaced request for
          an OpenBao service instance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoClusterClaimSpec defines the desired state of OpenBaoClusterClaim.
            properties:
              serviceOfferingRef:
                description: ServiceOfferingRef identifies the friendly stable service-offering
                  alias selected by the claim.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              serviceParameters:
                description: ServiceParameters carries the bounded claim-facing override
                  surface.
                properties:
                  backup:
                    description: Backup carries the bounded backup override surface.
                    properties:
                      location:
                        description: Location requests an allowed backup location
                          value when the resolved target permits it.
                        type: string
                      partition:
                        description: Partition requests an allowed backup partition
                          value when the resolved target permits it.
                        type: string
                    type: object
                  exposure:
                    description: Exposure carries the bounded exposure override surface.
                    properties:
                      hostname:
                        description: |-
                          Hostname requests a hostname when the selected exposure class allows
                          tenant-provided hostnames.
                        type: string
                    type: object
                type: object
              serviceProfileRef:
                description: ServiceProfileRef identifies the pinned immutable service-profile
                  revision requested by the claim or stored by the controller after
                  resolving ServiceOfferingRef.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              tenantRef:
                description: TenantRef identifies the tenant that governs this claim.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
            required:
            - tenantRef
            type: object
          status:
            description: OpenBaoClusterClaimStatus defines the observed state of OpenBaoClusterClaim.
            properties:
              applied:
                description: Applied summarizes the currently applied immutable revision
                  identities.
                properties:
                  approvedContract:
                    description: ApprovedContract identifies the currently applied
                      approved-service contract revision.
                    properties:
                      identityHash:
                        description: IdentityHash is the stable content hash for the
                          applied internal contract revision.
                        type: string
                    required:
                    - identityHash
                    type: object
                  backupProfileRef:
                    description: BackupProfileRef identifies the currently applied
                      immutable backup-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  bootstrapProfileRef:
                    description: BootstrapProfileRef identifies the currently applied
                      immutable bootstrap-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  exposureClassRef:
                    description: ExposureClassRef identifies the currently applied
                      immutable exposure-class revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  networkProfileRef:
                    description: NetworkProfileRef identifies the currently applied
                      immutable network-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  observabilityProfileRef:
                    description: ObservabilityProfileRef identifies the currently
                      applied immutable observability-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  renderedContract:
                    description: RenderedContract identifies the currently applied
                      rendered-execution contract revision.
                    properties:
                      identityHash:
                        description: IdentityHash is the stable content hash for the
                          applied internal contract revision.
                        type: string
                    required:
                    - identityHash
                    type: object
                  renderedDependencies:
                    description: RenderedDependencies identifies the currently applied
                      lower execution-policy dependency revisions.
                    properties:
                      backupAuthProfileRef:
                        description: BackupAuthProfileRef identifies the currently
                          applied immutable backup-auth-profile revision.
                        properties:
                          name:
                            description: Name is the immutable catalog object name.
                            type: string
                          uid:
                            description: UID is the Kubernetes object UID observed
                              when the claim bound this revision.
                            type: string
                        required:
                        - name
                        - uid
                        type: object
                      backupBackendRef:
                        description: BackupBackendRef identifies the currently applied
                          immutable backup-backend revision.
                        properties:
                          name:
                            description: Name is the immutable catalog object name.
                            type: string
                          uid:
                            description: UID is the Kubernetes object UID observed
                              when the claim bound this revision.
                            type: string
                        required:
                        - name
                        - uid
                        type: object
                      backupTargetRef:
                        description: BackupTargetRef identifies the currently applied
                          immutable backup-target revision.
                        properties:
                          name:
                            description: Name is the immutable catalog object name.
                            type: string
                          uid:
                            description: UID is the Kubernetes object UID observed
                              when the claim bound this revision.
                            type: string
                        required:
                        - name
                        - uid
                        type: object
                      bootstrapProjectionIdentity:
                        description: |-
                          BootstrapProjectionIdentity identifies the currently applied projected
                          bootstrap dependency artifact set for same-cluster execution.
                        properties:
                          identityHash:
                            description: IdentityHash is the stable content hash for
                              the applied internal contract revision.
                            type: string
                        required:
                        - identityHash
                        type: object
                      bootstrapProjectionRefs:
                        description: |-
                          BootstrapProjectionRefs identifies the concrete projected bootstrap
                          artifacts currently consumed by same-cluster execution.
                        items:
                          description: TypedObjectReference identifies another object
                            by kind, namespace, and name.
                          properties:
                            kind:
                              description: Kind is the referenced object kind.
                              minLength: 1
                              type: string
                            name:
                              description: Name is the referenced object name.
                              minLength: 1
                              type: string
                            namespace:
                              description: Namespace is the referenced object namespace
                                when applicable.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                        type: array
                      entrypointRef:
                        description: EntrypointRef identifies the currently applied
                          immutable entrypoint revision.
                        properties:
                          name:
                            description: Name is the immutable catalog object name.
                            type: string
                          uid:
                            description: UID is the Kubernetes object UID observed
                              when the claim bound this revision.
                            type: string
                        required:
                        - name
                        - uid
                        type: object
                      identity:
                        description: Identity identifies the currently applied rendered
                          dependency revision set.
                        properties:
                          identityHash:
                            description: IdentityHash is the stable content hash for
                              the applied internal contract revision.
                            type: string
                        required:
                        - identityHash
                        type: object
                      ingressPolicyRef:
                        description: IngressPolicyRef identifies the currently applied
                          immutable ingress-policy revision.
                        properties:
                          name:
                            description: Name is the immutable catalog object name.
                            type: string
                          uid:
                            description: UID is the Kubernetes object UID observed
                              when the claim bound this revision.
                            type: string
                        required:
                        - name
                        - uid
                        type: object
                      transferProfileRef:
                        description: TransferProfileRef identifies the currently applied
                          immutable transfer-profile revision.
                        properties:
                          name:
                            description: Name is the immutable catalog object name.
                            type: string
                          uid:
                            description: UID is the Kubernetes object UID observed
                              when the claim bound this revision.
                            type: string
                        required:
                        - name
                        - uid
                        type: object
                    type: object
                  runtimeProfileRef:
                    description: RuntimeProfileRef identifies the currently applied
                      immutable runtime-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  serviceOfferingRef:
                    description: ServiceOfferingRef identifies the currently applied
                      stable service-offering alias when the claim was bound through
                      one.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  serviceProfileRef:
                    description: ServiceProfileRef identifies the currently applied
                      immutable service-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  storageProfileRef:
                    description: StorageProfileRef identifies the currently applied
                      immutable storage-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  unsealProfileRef:
                    description: UnsealProfileRef identifies the currently applied
                      immutable unseal-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                  upgradePolicyRef:
                    description: UpgradePolicyRef identifies the currently applied
                      immutable upgrade-policy revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                type: object
              backup:
                description: Backup summarizes claim-facing backup lifecycle state
                  when available.
                properties:
                  consecutiveFailures:
                    description: ConsecutiveFailures is the number of consecutive
                      backup failures.
                    format: int32
                    type: integer
                  inProgress:
                    description: InProgress indicates whether a backup operation is
                      currently active.
                    type: boolean
                  lastAttemptTime:
                    description: LastAttemptTime is the timestamp of the last backup
                      attempt, regardless of outcome.
                    format: date-time
                    type: string
                  lastBackupDuration:
                    description: LastBackupDuration is how long the last successful
                      backup took.
                    type: string
                  lastBackupName:
                    description: LastBackupName is the object key/path of the last
                      successful backup.
                    type: string
                  lastBackupTime:
                    description: LastBackupTime is the timestamp of the last successful
                      backup.
                    format: date-time
                    type: string
                  lastFailureMessage:
                    description: LastFailureMessage is the detailed message for the
                      last backup failure.
                    type: string
                  lastFailureReason:
                    description: LastFailureReason is the low-cardinality reason for
                      the last backup failure.
                    type: string
                  nextScheduledBackup:
                    description: NextScheduledBackup is when the next backup is scheduled.
                    format: date-time
                    type: string
                  requestReason:
                    description: RequestReason explains the current workflow state.
                    type: string
                  requestRef:
                    description: RequestRef identifies the active immutable backup-request
                      object.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  requestState:
                    description: RequestState is the current workflow state reported
                      by the backup request.
                    enum:
                    - Pending
                    - Running
                    - Succeeded
                    - Blocked
                    - Failed
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the claim state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connection:
                description: Connection summarizes published connection details.
                properties:
                  caBundleRef:
                    description: CABundleRef identifies connection CA material when
                      it has been published.
                    properties:
                      kind:
                        description: Kind is the referenced object kind.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the referenced object namespace
                          when applicable.
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  endpoint:
                    description: Endpoint is the published user-facing endpoint when
                      available.
                    type: string
                  observedAt:
                    description: ObservedAt is when the connection contract was last
                      observed and published.
                    format: date-time
                    type: string
                  secretRef:
                    description: SecretRef identifies the claim-owned connection Secret
                      when it has been published.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                type: object
              materialization:
                description: Materialization summarizes the currently selected materialization
                  path.
                properties:
                  localRef:
                    description: LocalRef identifies the intended same-cluster concrete
                      workload object when materialization is local.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the referenced object namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  mode:
                    description: Mode identifies the materialization path currently
                      selected for the claim.
                    enum:
                    - SameCluster
                    type: string
                type: object
              observedGeneration:
                description: ObservedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the user-facing claim phase.
                enum:
                - Pending
                - Provisioning
                - Ready
                - Degraded
                - Failed
                - Deleting
                type: string
              restore:
                description: Restore summarizes the currently active restore workflow
                  when one exists.
                properties:
                  executionRef:
                    description: ExecutionRef identifies the active underlying OpenBaoRestore
                      execution object when one exists.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the referenced object namespace.
                        minLength: 1
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  message:
                    description: Message is the current best-effort restore workflow
                      message.
                    type: string
                  requestReason:
                    description: RequestReason explains the current request workflow
                      state.
                    type: string
                  requestRef:
                    description: RequestRef identifies the active immutable claim-restore-request
                      object when one exists.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  requestState:
                    description: RequestState is the current workflow state reported
                      by the claim restore request.
                    enum:
                    - Pending
                    - Running
                    - Succeeded
                    - Blocked
                    - Failed
                    type: string
                  snapshotKey:
                    description: SnapshotKey identifies the snapshot currently being
                      restored when available.
                    type: string
                  startTime:
                    description: StartTime is when the restore workflow started.
                    format: date-time
                    type: string
                  state:
                    description: State is the current workflow phase reported by the
                      underlying restore execution.
                    enum:
                    - Pending
                    - Validating
                    - Running
                    - Completed
                    - Failed
                    type: string
                type: object
              rollout:
                description: Rollout summarizes rollout progress for the claim.
                properties:
                  reason:
                    description: Reason provides the current rollout-state reason.
                    type: string
                  state:
                    description: State is the current rollout state.
                    enum:
                    - Idle
                    - Pending
                    - Rendering
                    - RollingOut
                    - Blocked
                    - Failed
                    type: string
                type: object
              summary:
                description: Summary is the current best-effort diagnostic summary
                  when the claim is not in a steady ready state.
                properties:
                  message:
                    description: Message is the current best-effort human-readable
                      summary.
                    type: string
                  reason:
                    description: Reason is a low-cardinality reason for the current
                      summary.
                    type: string
                  severity:
                    description: Severity classifies the current summary.
                    enum:
                    - Info
                    - Warning
                    - Error
                    type: string
                  sourceRef:
                    description: SourceRef identifies the object currently driving
                      this summary when available.
                    properties:
                      kind:
                        description: Kind is the referenced object kind.
                        minLength: 1
                        type: string
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace is the referenced object namespace
                          when applicable.
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                type: object
              upgrade:
                description: Upgrade summarizes the currently active upgrade workflow
                  when one exists.
                properties:
                  classification:
                    description: Classification summarizes the evaluated compatibility
                      class when available.
                    properties:
                      class:
                        description: Class is the evaluated compatibility class.
                        enum:
                        - InPlace
                        - Blocked
                        type: string
                      reason:
                        description: Reason explains the evaluated compatibility class.
                        type: string
                    type: object
                  reason:
                    description: Reason explains the current workflow state.
                    type: string
                  requestRef:
                    description: RequestRef identifies the active immutable upgrade-request
                      object.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  state:
                    description: State is the current workflow state reported by the
                      upgrade request.
                    enum:
                    - Pending
                    - RollingOut
                    - Succeeded
                    - Blocked
                    - Failed
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
