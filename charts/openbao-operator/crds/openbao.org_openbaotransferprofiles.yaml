---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openbaotransferprofiles.openbao.org
spec:
  group: openbao.org
  names:
    kind: OpenBaoTransferProfile
    listKind: OpenBaoTransferProfileList
    plural: openbaotransferprofiles
    singular: openbaotransferprofile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.partSize
      name: Part Size
      type: integer
    - jsonPath: .spec.concurrency
      name: Concurrency
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoTransferProfile is the immutable platform-owned transfer
          tuning object.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoTransferProfileSpec defines the desired state of OpenBaoTransferProfile.
            properties:
              concurrency:
                default: 3
                description: Concurrency is the number of concurrent multipart uploads.
                format: int32
                maximum: 10
                minimum: 1
                type: integer
              partSize:
                default: 10485760
                description: PartSize is the multipart upload part size in bytes.
                format: int64
                minimum: 5242880
                type: integer
            type: object
            x-kubernetes-validations:
            - message: spec is immutable after creation
              rule: self == oldSelf
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
