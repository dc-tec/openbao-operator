# This patch enables the metrics endpoint on port 8443
# It adds --metrics-bind-address=:8443 to the container args
- op: add
  path: /spec/template/spec/containers/0/args/-
  value: --metrics-bind-address=:8443
