---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openbaoclusterclaimupgraderequests.openbao.org
spec:
  group: openbao.org
  names:
    kind: OpenBaoClusterClaimUpgradeRequest
    listKind: OpenBaoClusterClaimUpgradeRequestList
    plural: openbaoclusterclaimupgraderequests
    shortNames:
    - obclaimupgrade
    singular: openbaoclusterclaimupgraderequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.claimRef.name
      name: Claim
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.classification.class
      name: Class
      priority: 1
      type: string
    - jsonPath: .status.target.serviceProfileRef.name
      name: Target
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoClusterClaimUpgradeRequest is the immutable workflow request
          for post-materialization claim upgrades.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoClusterClaimUpgradeRequestSpec defines the desired
              state of OpenBaoClusterClaimUpgradeRequest.
            properties:
              claimRef:
                description: ClaimRef identifies the namespaced claim this request
                  targets.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              target:
                description: Target identifies the requested upgrade target.
                properties:
                  serviceOfferingRef:
                    description: ServiceOfferingRef identifies the stable service-offering
                      alias requested for upgrade.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  serviceProfileRef:
                    description: ServiceProfileRef identifies the explicit immutable
                      service-profile revision requested for upgrade.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one of serviceOfferingRef or serviceProfileRef
                    must be set
                  rule: has(self.serviceOfferingRef) != has(self.serviceProfileRef)
            required:
            - claimRef
            - target
            type: object
            x-kubernetes-validations:
            - message: exactly one of spec.target.serviceOfferingRef or spec.target.serviceProfileRef
                must be set
              rule: has(self.target.serviceOfferingRef) != has(self.target.serviceProfileRef)
          status:
            description: OpenBaoClusterClaimUpgradeRequestStatus defines the observed
              state of OpenBaoClusterClaimUpgradeRequest.
            properties:
              classification:
                description: Classification summarizes the evaluated compatibility
                  class when available.
                properties:
                  class:
                    description: Class is the evaluated compatibility class.
                    enum:
                    - InPlace
                    - ReplacementRequired
                    - Blocked
                    type: string
                  reason:
                    description: Reason explains the evaluated compatibility class.
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the request state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              current:
                description: Current summarizes the currently applied claim revision
                  state when available.
                properties:
                  approvedContract:
                    description: ApprovedContract identifies the resolved approved
                      service contract revision.
                    properties:
                      identityHash:
                        description: IdentityHash is the stable content hash for the
                          applied internal contract revision.
                        type: string
                    required:
                    - identityHash
                    type: object
                  renderedContract:
                    description: RenderedContract identifies the resolved rendered
                      execution contract revision when available.
                    properties:
                      identityHash:
                        description: IdentityHash is the stable content hash for the
                          applied internal contract revision.
                        type: string
                    required:
                    - identityHash
                    type: object
                  serviceOfferingRef:
                    description: ServiceOfferingRef identifies the stable offering
                      alias involved in the request or applied state when relevant.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  serviceProfileRef:
                    description: ServiceProfileRef identifies the resolved immutable
                      service-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                type: object
              observedGeneration:
                description: ObservedGeneration is the latest request generation observed
                  by the controller.
                format: int64
                type: integer
              reason:
                description: Reason explains the current workflow state.
                type: string
              state:
                description: State is the current request workflow state.
                enum:
                - Pending
                - Classifying
                - RollingOut
                - Succeeded
                - ReplacementRequired
                - Blocked
                - Failed
                type: string
              target:
                description: Target summarizes the resolved target revision state
                  when available.
                properties:
                  approvedContract:
                    description: ApprovedContract identifies the resolved approved
                      service contract revision.
                    properties:
                      identityHash:
                        description: IdentityHash is the stable content hash for the
                          applied internal contract revision.
                        type: string
                    required:
                    - identityHash
                    type: object
                  renderedContract:
                    description: RenderedContract identifies the resolved rendered
                      execution contract revision when available.
                    properties:
                      identityHash:
                        description: IdentityHash is the stable content hash for the
                          applied internal contract revision.
                        type: string
                    required:
                    - identityHash
                    type: object
                  serviceOfferingRef:
                    description: ServiceOfferingRef identifies the stable offering
                      alias involved in the request or applied state when relevant.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  serviceProfileRef:
                    description: ServiceProfileRef identifies the resolved immutable
                      service-profile revision.
                    properties:
                      name:
                        description: Name is the immutable catalog object name.
                        type: string
                      uid:
                        description: UID is the Kubernetes object UID observed when
                          the claim bound this revision.
                        type: string
                    required:
                    - name
                    - uid
                    type: object
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
