---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openbaoclusterclaimrestorerequests.openbao.org
spec:
  group: openbao.org
  names:
    kind: OpenBaoClusterClaimRestoreRequest
    listKind: OpenBaoClusterClaimRestoreRequestList
    plural: openbaoclusterclaimrestorerequests
    shortNames:
    - obclaimrestore
    singular: openbaoclusterclaimrestorerequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.claimRef.name
      name: Claim
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.snapshotKey
      name: Snapshot
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoClusterClaimRestoreRequest is the immutable workflow request
          for one-shot same-cluster claim restores.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoClusterClaimRestoreRequestSpec defines the desired
              state of OpenBaoClusterClaimRestoreRequest.
            properties:
              claimRef:
                description: ClaimRef identifies the namespaced claim this request
                  targets.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              source:
                description: Source selects the claim backup to restore. When omitted,
                  the request restores the latest successful backup recorded on the
                  claim-managed local cluster.
                properties:
                  backupRequestRef:
                    description: |-
                      BackupRequestRef identifies a completed OpenBaoClusterClaimBackupRequest for the same claim.
                      Required when mode is BackupRequest.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  mode:
                    description: Mode selects how the restore source is resolved.
                      Omitted mode defaults to LatestSuccessful.
                    enum:
                    - LatestSuccessful
                    - BackupRequest
                    type: string
                type: object
                x-kubernetes-validations:
                - message: source.mode must be BackupRequest when source.backupRequestRef
                    is set
                  rule: '!has(self.backupRequestRef) || (has(self.mode) && self.mode
                    == ''BackupRequest'')'
                - message: source.backupRequestRef is required when source.mode is
                    BackupRequest
                  rule: '!has(self.mode) || self.mode != ''BackupRequest'' || has(self.backupRequestRef)'
                - message: source.backupRequestRef must be omitted when source.mode
                    is LatestSuccessful
                  rule: '!has(self.mode) || self.mode != ''LatestSuccessful'' || !has(self.backupRequestRef)'
            required:
            - claimRef
            type: object
          status:
            description: OpenBaoClusterClaimRestoreRequestStatus defines the observed
              state of OpenBaoClusterClaimRestoreRequest.
            properties:
              clusterRef:
                description: ClusterRef identifies the resolved local cluster targeted
                  by this request.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the referenced object namespace.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              completionTime:
                description: CompletionTime is when the request reached a terminal
                  state.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the request state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the latest request generation observed
                  by the controller.
                format: int64
                type: integer
              reason:
                description: Reason explains the current workflow state.
                type: string
              restoreRef:
                description: RestoreRef identifies the underlying OpenBaoRestore execution
                  object when one exists.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the referenced object namespace.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              snapshotKey:
                description: SnapshotKey identifies the snapshot object key restored
                  by this request when available.
                type: string
              startTime:
                description: StartTime is when the restore attempt associated with
                  this request started.
                format: date-time
                type: string
              state:
                description: State is the current request workflow state.
                enum:
                - Pending
                - Running
                - Succeeded
                - Blocked
                - Failed
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
