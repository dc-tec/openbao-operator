---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openbaoserviceofferings.openbao.org
spec:
  group: openbao.org
  names:
    kind: OpenBaoServiceOffering
    listKind: OpenBaoServiceOfferingList
    plural: openbaoserviceofferings
    singular: openbaoserviceoffering
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.currentRevisionRef.name
      name: Revision
      type: string
    - jsonPath: .spec.lifecycle.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoServiceOffering is the mutable stable alias that points
          claim users at a current immutable service-profile revision.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenBaoServiceOfferingSpec defines the desired state of OpenBaoServiceOffering.
            properties:
              currentRevisionRef:
                description: CurrentRevisionRef identifies the current immutable service-profile
                  revision for new claim bindings.
                properties:
                  name:
                    description: Name is the referenced object name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              lifecycle:
                description: Lifecycle captures optional lifecycle metadata for this
                  stable offering alias.
                properties:
                  replacementRef:
                    description: ReplacementRef identifies the preferred replacement
                      offering when this one is deprecated or replaced.
                    properties:
                      name:
                        description: Name is the referenced object name.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  state:
                    default: Active
                    description: State summarizes whether the offering remains active
                      for new bindings.
                    enum:
                    - Active
                    - Deprecated
                    - Replaced
                    type: string
                type: object
            required:
            - currentRevisionRef
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
