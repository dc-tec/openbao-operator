---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: openbaoupgradepolicies.openbao.org
spec:
  group: openbao.org
  names:
    kind: OpenBaoUpgradePolicy
    listKind: OpenBaoUpgradePolicyList
    plural: openbaoupgradepolicies
    singular: openbaoupgradepolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenBaoUpgradePolicy is the immutable platform-owned upgrade-policy
          catalog object.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              OpenBaoUpgradePolicySpec defines platform-owned upgrade behavior for
              catalog-backed OpenBao clusters.
            properties:
              blueGreen:
                description: |-
                  BlueGreen defines bounded blue/green behavior. It is applied when the
                  service profile selects the BlueGreen upgrade strategy.
                properties:
                  autoPromote:
                    description: |-
                      AutoPromote controls whether the controller promotes the candidate
                      deployment automatically after verification.
                    type: boolean
                  autoRollback:
                    description: |-
                      AutoRollback controls rollback behavior when blue/green verification or
                      promotion fails.
                    properties:
                      enabled:
                        description: Enabled controls whether automatic rollback is
                          enabled.
                        type: boolean
                      onJobFailure:
                        description: OnJobFailure rolls back when verification jobs
                          exceed MaxJobFailures.
                        type: boolean
                      onValidationFailure:
                        description: OnValidationFailure rolls back when validation
                          fails.
                        type: boolean
                    type: object
                  maxJobFailures:
                    description: |-
                      MaxJobFailures limits verification job failures before the upgrade is
                      marked failed.
                    format: int32
                    type: integer
                  minSyncDuration:
                    description: |-
                      MinSyncDuration is the minimum time the candidate should stay in sync
                      before promotion.
                    type: string
                type: object
            type: object
            x-kubernetes-validations:
            - message: spec is immutable after creation
              rule: self == oldSelf
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
