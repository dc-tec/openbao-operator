version: v1
generatedAt: 2026-02-10T00:00:00Z
nodeImage: kindest/node:v1.34.3
multipliers:
    p95: 1.25
    max: 1.4
metricSchema:
    - reconcile_p95_seconds
    - backup_last_duration_seconds_max
    - restore_p95_seconds
    - upgrade_p95_seconds
    - upgrade_pod_p95_seconds
    - workqueue_retries_delta
    - reconcile_error_ratio
scenarios:
    backup-restore:
        labelFilter: dr && backup && restore && !failure-injection
        metrics:
            backup_last_duration_seconds_max: 308
            reconcile_error_ratio: 1
            reconcile_p95_seconds: 75
            restore_p95_seconds: 1500
            upgrade_p95_seconds: 0
            upgrade_pod_p95_seconds: 0
            workqueue_retries_delta: 196
    lifecycle:
        labelFilter: lifecycle && critical && tenant && !slow && !openshift && !pentest
        metrics:
            backup_last_duration_seconds_max: 0
            reconcile_error_ratio: 1
            reconcile_p95_seconds: 38
            restore_p95_seconds: 0
            upgrade_p95_seconds: 0
            upgrade_pod_p95_seconds: 0
            workqueue_retries_delta: 62
    rolling-upgrade:
        labelFilter: upgrade && rolling && !failure && !bluegreen
        metrics:
            backup_last_duration_seconds_max: 0
            reconcile_error_ratio: 1
            reconcile_p95_seconds: 82
            restore_p95_seconds: 0
            upgrade_p95_seconds: 2250
            upgrade_pod_p95_seconds: 750
            workqueue_retries_delta: 140
