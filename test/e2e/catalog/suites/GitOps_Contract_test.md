# GitOps contract (Argo-like apply)

Source: `test/e2e/GitOps_Contract_test.go`

Note: recorded checkpoints are best-effort extracts from literal `By(...)` calls visible to `ginkgo outline`.

## Cases

| Case ID | Spec | State | Covers | Labels |
| --- | --- | --- | --- | --- |
| `gitops-ssa-drift-contract` | repeatedly applies the same manifest without spec/metadata drift | active | `gitops-drift`, `server-side-apply` | `gitops`, `contract`, `lower-layer-covered` |

## `gitops-ssa-drift-contract`

Path: `GitOps contract (Argo-like apply) > repeatedly applies the same manifest without spec/metadata drift`

State: `active`

Generated fallback ID: `gitops-contract-repeatedly-applies-the-same-manifest-without-43cd8392`

Covers: `gitops-drift`, `server-side-apply`

Labels: `gitops`, `contract`, `lower-layer-covered`


